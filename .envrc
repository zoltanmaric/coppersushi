# Activate the uv-created virtualenv when entering this directory.
# Requires direnv (https://direnv.net) and a one-off `direnv allow`.
# No-op for Conda/Mamba users, who have no .venv/.
if [ -d "$PWD/.venv" ]; then
  export VIRTUAL_ENV="$PWD/.venv"
  PATH_add "$PWD/.venv/bin"
fi
