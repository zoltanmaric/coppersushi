# Networks Guidelines

- **sanction-by-hand** — Solved networks here are sanctioned by the user. The runner writes every solve to the gitignored `candidates/`; `promote` copies one to `opf-<day>.nc` only on the user's say-so, and committing it is the sanction. Each committed version becomes a Git LFS object that GitHub keeps forever, so promote sparingly.
